# سجل التحقق

تمت معاينة الصفحة على سطح المكتب والهاتف. الواجهة تعرض بنجاح التخطيط العربي RTL، شريط التنقل، بطاقات الخدمات، قسم الدفع، نموذج الدعم، والتذييل. كما تم التأكد من أن معاينات الفيديو في المعرض تستخدم ملفًا مخزنًا في مسار تخزين المشروع وتصرح بوضوح بأنها عينات عامة وليست أعمال عملاء.

تمت محاولة إرسال نموذج الدعم ونموذج الإيصال دون تعبئة الحقول الإلزامية. أبقى المتصفح الطلبات داخل التحقق المحلي ولم تُنشأ سجلات أو إشعارات. ما زال اختبار نجاح الإرسال الفعلي مؤجلًا لتجنب إنشاء بيانات اختبارية في قاعدة بيانات المشروع.

تم تأكيد انتقال الاستبيان من السؤال الأول إلى الثاني داخل المتصفح. كما تم التحقق من أن بطاقات UGC الثلاث تحمل فيديو التخزين المُدار نفسه بحالة جاهزية كاملة (`readyState = 4`) مع عناصر تحكم تشغيل ظاهرة للمستخدم.

تمت معاينة الصفحات المستقلة: الرئيسية والخدمات وسابقة الأعمال وطرق الدفع والدعم الفني، على سطح المكتب والهاتف. الهوية الداكنة والأزرق الملكي والأصفر والبنفسجي ظاهرة بوضوح، والبطاقات تستخدم شبكة متعددة الأعمدة على الشاشات العريضة مع تخطيط قابل للقراءة على الهاتف. تظهر معاينات الفيديو بحالة انتظار مؤقتة في لقطات الهاتف، وتبقى عناصر التحكم متاحة عند اكتمال التحميل.

بعد التحديث، تمت معاينة الصفحة الرئيسية مجددًا على سطح المكتب والهاتف. يظهر قسم «عن MSB Media» في أسفل الصفحة بصورة منفصلة للمؤسس وصورة منفصلة لتصور الوكالة، إلى جانب السطر الإنجليزي المطلوب. كما بقيت الشبكات والهوية الداكنة وواجهة الاستبيان مستقرة بصريًا.

تم التحقق من مسار المساعد الاستشاري عبر طلب محلي فعلي لسؤال عن منصة تعليمية؛ أعاد المساعد مرحلة الاستماع (`discover`) مع تعطيل التحويل إلى واتساب، وهو السلوك المطلوب قبل اكتمال التأهيل. كما ظهر زر «استشارة ذكية» في الواجهة الرئيسية دون حجب المحتوى.

تمت معاينة الصفحة الرئيسية وصفحة الخدمات بعد التحديث الأخير. ظهرت بطاقات خدمات الذكاء الاصطناعي والترجمة الجديدة بأسعارها المعتمدة، ورموز بجوار النص، ومساحة مراجعات تؤكد الاقتصار على المراجعات الحقيقية والمعتمدة. كما ظهر زر مشاركة الموقع في التذييل، وحافظت أزرار واتساب على هوية الأيقونة الخضراء.

أعيد فحص الرئيسية والخدمات على شاشة هاتف بعرض 375 بكسل. حافظت البطاقات على تسلسل واضح، وأصبح التدقيق التسويقي من ثلاث خطوات فقط وينتهي بزر «واتساب». كما فُعّلت حركة ظهور خفيفة في أقسام الخدمات وعرض الفيديو مع احترام تفضيل تقليل الحركة.

تمت معاينة الفيديو المرفق داخل الرئيسية وصفحة الخدمات. يظهر الفيديو ضمن إطار إعلاني مستقل مع عناصر تحكم التشغيل المحلية وزر تحويل واضح إلى واتساب، وبقيت البطاقات والشبكات في الصفحات قابلة للقراءة.

بعد التحديث، ظهر الفيديو ضمن إطار طولي بنسبة 9:16 يعرض المحتوى كاملًا من دون قص، مع زر واتساب أصفر عائم في أسفل يسار الفيديو وسهم توجيهي متحرك. تم التحقق من النتيجة في الرئيسية وصفحة الخدمات على سطح المكتب.

تم فتح الصفحة الرئيسية بعد تثبيت Meta Pixel وتأكيد أن `window.fbq` دالة متاحة في المتصفح. يدعم التتبع حدث PageView الأولي، وحدث Contact قبل فتح واتساب، وحدث CompleteRegistration بعد إنشاء اشتراك جديد فقط. كما تم تحديث روابط التذييل لتضم صفحة الوكالة الجديدة والحساب الشخصي بشكل منفصل.

تم فصل محتوى الموقع بحيث أصبحت الرئيسية مختصرة، وأصبحت الخدمات والأعمال والدفع والدعم والتعريف بالفريق صفحات مستقلة من الشريط الجانبي على سطح المكتب وقائمة الهاتف. تمت إزالة واجهة المساعد غير المكتمل حتى لا يعتمد العميل على خدمة ذكاء اصطناعي غير مهيأة خارجياً.

تم إنشاء مشروع Supabase نشط، وجداول العملاء والإيصالات وطلبات الدعم والجلسات، مع تفعيل RLS ومنع أي وصول مباشر عام إلى الجداول. حفظ الإيصالات يتم في حاوية خاصة غير عامة. نجحت دالة `msb-data-api` في الاستجابة الآمنة لطلب غير صحيح دون إنشاء بيانات اختبارية. تظهر ملاحظات RLS المعلوماتية في مدقق Supabase لأن عدم وجود سياسات عامة مقصود لمنع العميل من الوصول المباشر للبيانات؛ الدالة الخادمية فقط تستخدم صلاحية الخادم.

تم تنفيذ `pnpm check` و`pnpm test` و`pnpm build:vercel` بنجاح بعد التغييرات الأخيرة؛ اجتازت 9 اختبارات. تمت معاينة الرئيسية والدفع والدعم بصريًا على سطح المكتب، وظهر نموذج الإيصال بزر مشاركة واتساب وزر حفظ آمن واضحين دون مساعد عائم.

تم نشر الإصدار الخامس النشط من دالة Supabase `msb-data-api`، وهو يحفظ التسجيلات وطلبات الدعم والإيصالات ثم يحاول إرسال إشعار إلى بريد المالك عبر Resend من دون أن يعطل حفظ البيانات عند فشل البريد. تم حفظ `RESEND_API_KEY` و`RESEND_FROM` كأسرار داخل Supabase فقط، ولم تُضف إلى ملفات المشروع أو GitHub. تم تأكيد أن بريد Resend نفسه يستقبل رسائل الحساب، بينما يبقى اختبار أول إشعار صادر عن بيانات عميل حقيقية مؤجلاً تجنبًا لإنشاء سجلات اختبارية.

تمت مراجعة الرئيسية والخدمات والدفع عند عروض 375×812 للهاتف وiPhone، و768×1024 للتابلت، و1024×768 للابتوب، و1440×900 لسطح المكتب. القائمة تتحول إلى تنقل مناسب للمساحة المتاحة، بينما تظهر البطاقات والنماذج دون قص أفقي أو تراكب ملحوظ، ويظهر الشريط الجانبي على الشاشات العريضة.

أظهرت المراجعة البصرية التفصيلية أن الهاتف يعرض زر القائمة بدل التنقل العريض، وتبقى أزرار واتساب وحفظ الإيصال ضمن عرض الشاشة. وعلى التابلت واللابتوب تتسع البطاقات والنماذج من دون تجاوز أفقي، بينما تستعيد الشاشات العريضة شبكة البطاقات ومساحة نموذج الدفع الكاملة. لا تُظهر لقطات الصفحة الكاملة العناصر الثابتة غير العلوية بحسب طريقة الالتقاط؛ لذلك لا تُعدّ غيابها من لقطة كاملة مشكلة في الواجهة الفعلية.

أضيفت حماية فعلية ضد تجاوز العرض عبر `box-sizing` العام و`overflow-x: clip`، ودعم هوامش الشاشة الآمنة لأجهزة iPhone في الشريط العلوي والقائمة، مع التفاف آمن لمحتوى الدفع والرفع على الشاشات الصغيرة. نجح فحص TypeScript وبناء Vercel بعد هذه التعديلات. وتمت معاينة / و/services و/payment مجددًا عند 390×844 للهاتف، و768×1024 للتابلت، و1440×900 للشاشات العريضة؛ لم يظهر قص أو تراكب أو تمرير أفقي غير مقصود ضمن المحتوى.

تم رفع النسخة النهائية بنجاح إلى الفرع `main` في مستودع GitHub العام `mohamedsayedmsb1999-dev/msbmedia` بالالتزام باستخدام اعتماد المستخدم المصادق عليه. لم يظهر Webhook علني لـ Vercel عند فحص المستودع، وهو لا يكفي بمفرده للحكم على حالة النشر.

تم فحص الدومين الحي مباشرة في 23 أغسطس 2026. يحوّل `https://msbmedia.agency` عبر خادم Vercel إلى `https://www.msbmedia.agency`، وتعرض الوجهة موقع MSB Media الحالي بعناصره المعتمدة: القائمة العربية، الصفحة الرئيسية المختصرة، التسجيل/الدخول، زر واتساب، وروابط صفحات الخدمات والأعمال والدفع والدعم والتعريف. لم يُجرَ أي حذف أو تعديل على مشروع MSB2 Media أو أي مشروع آخر أثناء هذا التحقق.
