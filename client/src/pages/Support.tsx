import SiteShell from "@/components/SiteShell";
import { FormEvent, useState } from "react";
import { Headphones, Mail, Send, TicketCheck } from "lucide-react";
import { toast } from "sonner";
import { trpc } from "@/lib/trpc";
import { openWhatsApp } from "@/lib/site-data";

export default function Support() { const [form, setForm] = useState({ name: "", phone: "", email: "", subject: "", message: "" }); const support = trpc.support.create.useMutation({ onSuccess: data => { toast.success("تم حفظ طلبك وفتح مسار البريد المباشر."); window.location.assign(data.mailUrl); setForm({ name: "", phone: "", email: "", subject: "", message: "" }); }, onError: err => toast.error(err.message) }); const submit = (event: FormEvent) => { event.preventDefault(); support.mutate(form); }; return <SiteShell><section className="page-hero"><div className="site-container"><span className="eyebrow"><Headphones className="h-4 w-4" />الدعم الفني</span><h1>لا نترك سؤالك معلقًا</h1><p>سجّل التفاصيل، وسيصل طلبك إلى النظام مع تنبيه لفريقنا ونافذة بريد مباشرة.</p></div></section><section className="site-container grid gap-8 py-16 lg:grid-cols-[.8fr_1.2fr]"><aside className="support-aside"><TicketCheck className="h-9 w-9 text-[#ffd400]" /><h2>طلب واضح، متابعة أسرع</h2><p>كل طلب يحتفظ بعنوانه وتفاصيله ويرسل تنبيهًا للفريق. للحالات العاجلة استخدم محادثة واتساب.</p><button className="yellow-button mt-6" onClick={() => openWhatsApp("أحتاج دعمًا فنيًا")}>تواصل عبر واتساب</button><a className="mt-4 flex items-center gap-2 text-sm text-blue-300" href="mailto:mohamedsayedmsb1999@gmail.com"><Mail className="h-4 w-4" />البريد المباشر للدعم</a></aside><form onSubmit={submit} className="support-form"><h2>افتح طلب دعم</h2><p>سنرتب التفاصيل قبل فتح تطبيق البريد لتصل رسالتك بصيغة واضحة.</p><div className="form-grid"><label>الاسم<input required className="dark-input" value={form.name} onChange={e => setForm({ ...form, name: e.target.value })} /></label><label>رقم الهاتف<input required className="dark-input" inputMode="tel" value={form.phone} onChange={e => setForm({ ...form, phone: e.target.value })} /></label><label>البريد (اختياري)<input className="dark-input" type="email" value={form.email} onChange={e => setForm({ ...form, email: e.target.value })} /></label><label>عنوان الطلب<input required className="dark-input" value={form.subject} onChange={e => setForm({ ...form, subject: e.target.value })} /></label></div><label className="mt-5 block">تفاصيل الطلب<textarea required minLength={10} className="dark-input min-h-36" value={form.message} onChange={e => setForm({ ...form, message: e.target.value })} /></label><button disabled={support.isPending} className="blue-button mt-6">{support.isPending ? "جارٍ الإرسال..." : <><Send className="h-4 w-4" />إرسال وفتح البريد</>}</button></form></section></SiteShell>; }
