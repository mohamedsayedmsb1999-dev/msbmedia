import SiteShell from "@/components/SiteShell";
import { openWhatsApp, services } from "@/lib/site-data";
import { ArrowLeft, CheckCircle2, Sparkles } from "lucide-react";
import PromoVideo from "@/components/PromoVideo";

export default function Services() { return <SiteShell><section className="page-hero"><div className="site-container"><span className="eyebrow"><Sparkles className="h-4 w-4" />خدماتنا</span><h1>حلول متصلة، لا مهام منفصلة</h1><p>نحن نرتّب الأدوات والإبداع والأداء داخل مسار يناسب هدف مشروعك.</p></div></section><section className="site-container py-16"><div className="service-grid">{services.map(service => { const Icon = service.icon; return <article key={service.title} className={`service-card ${service.tone}`}><div className="flex items-start justify-between gap-3"><span className="icon-box"><Icon className="h-6 w-6" /></span>{service.price && <span className="price-badge">{service.price}</span>}</div><h2>{service.title}</h2><p>{service.copy}</p>{service.tone === "ai" && <div className="mt-5 flex flex-wrap gap-2">{["Gemini PRO", "Google Flow", "Veo 3", "Nano Banana", "Cloud Storage"].map(item => <span className="ai-badge" key={item}>{item}</span>)}</div>}<button onClick={() => openWhatsApp(`أريد البدء في خدمة ${service.title}`)} className="mt-7 inline-flex items-center gap-2 text-sm font-black">ابدأ <ArrowLeft className="h-4 w-4" /></button></article>; })}</div><div className="mt-10"><PromoVideo compact /></div><div className="mt-8 grid gap-4 md:grid-cols-3">{["نختار القناة المناسبة للهدف", "ننفذ ونراجع كل خطوة", "نحوّل التعلم إلى قرار تالي"].map(item => <div className="mini-process" key={item}><CheckCircle2 className="h-5 w-5 text-[#ffd400]" />{item}</div>)}</div></section></SiteShell>; }
